### 強引用 (Strong Reference)

- **機制**：這是預設的引用類型，指向物件的「所有權」聲明。。當你將一個物件實例賦值給一個變數或常數時，預設就是建立一個強引用。

- **作用**：只要至少還有一個強引用指向某個物件，ARC 就會確保該物件持續存在於記憶體中，不會被銷毀。可以說，強引用「擁有」它所指向的物件。

- **範例**：

   ```swift
   var user: User? = User(name: "John") // 建立一個對 User 物件的強引用
   // 此時 User 物件的引用計數為 1
   ```
### 弱引用 (Weak Reference)

- **機制**：使用 `weak` 關鍵字來宣告。弱引用不會增加物件的引用計數。

- **作用**：它允許你引用一個物件，但不會阻止該物件被銷毀。如果它引用的物件被銷毀了，ARC 會自動將這個弱引用變數設為 `nil`。

- **設計**：正因為可能在任何時候變成 `nil`，弱引用變數**必須**是可選類型 (Optional)。

- **使用場景**：主要用來解決「強引用循環」(Strong Reference Cycle) 的問題，例如 `delegate` 模式或兩個物件互相引用的情況。